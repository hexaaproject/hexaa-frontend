{# 
usage in twig:
{{ include('::panel.html.twig', {heading: 'foobar', buttonappear:'none/inline' main: service/organization, items: items}) }}

usage in symfony (example:key1=Name, value1=name):
return $this->render(
            'AppBundle:Whatever.html.twig',
            array(
                "items" => array(
                    "key1" => "value1", 
                    "key2" => "value2"
                    .
                    .
                    .
                ),
                "main" => service/organization
            )
        );
#}
<script>
    function edit(){
        $(".propdescription").prop('readonly', false);
        $(".sendbutton")[0].style.display="inline";
    }
</script>
<div class="propertiespanel">
    <div class="header">
        {{heading}}
        <button type="submit" class="sendbutton pull-right"  style="display:none"><i class="material-icons">done</i></button>
        <button type="button" class="editbutton" style="display: {{buttonappear}}" onclick="edit()"><i class="material-icons">create</i></button>
   
    </div>
    {% for key, value in items %}
        {% if main[value]%}
            <div>
                <div class="propname">{{key}}</div>
                <textarea class="propdescription" readonly cols="30" rows="1" name="{{key}}"> {{main[value]}} </textarea>
            </div>
        {% else %}
             <div>
                <div class="propname">{{key}}</div>
                <textarea class="propdescription" readonly cols="30" rows="1" name="{{key}}"> </textarea>               
                {#<div class="propdescription" >{{main[value]}}</div>#}
            </div>
        {% endif %}
    {% endfor %}
</div>

