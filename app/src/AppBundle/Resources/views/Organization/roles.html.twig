{% extends "AppBundle:Organization:layout.html.twig" %}

{% block container %}
    {% block stylesheets %}
        {% stylesheets
            'css/accordions.css'
            filter='cssrewrite'
        %}
            <link rel="stylesheet" href="{{ asset_url }}" />
        {% endstylesheets %}
    {% endblock %}
    

    <div class="row" style="margin-bottom: 24px;">
        <div class="col-md-4 col-md-offset-3">
            <button class="btnhexaa btn-red btn-block btnhexaa-lg"><i class="glyphicon glyphicon-plus" aria-hidden="true"></i> New role</button>
        </div>
        <div class="col-md-4 col-md-offset-1">
            <button class="btnhexaa btn-red-inverse btn-block btnhexaa-lg">Add role to user</button>
        </div>
    </div>
    <div class="row">
        <div class="col-md-11 col-md-offset-2">
            {{ include('::accordions.html.twig', { heading: "Roles", variant: "dark", items: roles_accordion }) }}
        </div>
    </div>
{% endblock %}
